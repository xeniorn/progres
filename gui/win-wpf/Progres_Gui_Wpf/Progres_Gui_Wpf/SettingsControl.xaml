<UserControl x:Class="Progres_Gui_Wpf.SettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Progres_Gui_Wpf"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance local:MainWindowViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="local:TextInputBox">
            <Setter Property="Margin" Value="2"></Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <WrapPanel>
            <local:TextInputBox MinWidth="200" Label="Models path" Text="{Binding DownloadedModelsPath}"></local:TextInputBox>
            <local:TextInputBox MinWidth="50" Label="Docker image" Text="{Binding DockerImageName}"></local:TextInputBox>
            <Button Content="Install Docker" Click="InstallDocker_OnClick"></Button>

        </WrapPanel>
    </Grid>
</UserControl>
